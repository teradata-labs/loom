# Test agent config for unit tests
apiVersion: loom/v1
kind: Agent
metadata:
  name: Test Agent 1
  version: 1.0.0
  description: A test agent for validating path loading
  labels:
    test: "true"
    category: testing
spec:
  system_prompt: |
    You are a test agent used for validating agent path loading functionality.
    This is the base system prompt from the agent config file.
  tools:
    - test_tool_1
    - test_tool_2
