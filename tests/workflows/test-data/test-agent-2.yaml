# Test agent config for unit tests
apiVersion: loom/v1
kind: Agent
metadata:
  name: Test Agent 2
  version: 1.0.0
  description: Another test agent for validating path loading
spec:
  system_prompt: |
    You are test agent 2.
    This agent has fewer tools than test agent 1.
  tools:
    - test_tool_3
