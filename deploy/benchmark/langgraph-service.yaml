apiVersion: v1
kind: Service
metadata:
  name: langgraph-bench-server
  namespace: loom-bench
spec:
  type: ClusterIP
  selector:
    app: langgraph-bench-server
  ports:
    - name: grpc
      port: 50051
      targetPort: grpc
      protocol: TCP
