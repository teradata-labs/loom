apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bench-results
  namespace: loom-bench
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: managed-csi
  resources:
    requests:
      storage: 10Gi
